<apex:page standardController="Job_Poojan__c">
  <apex:form >
    <apex:pageBlock title="Job_Poojan Edit" mode="edit">
       <apex:pageBlockButtons > 
        <apex:commandButton action="{!save}" value="Save"/>
        <apex:commandButton action="{!edit}" value="Edit"/>
      </apex:pageBlockButtons>
      <apex:pageBlockSection title="Information" columns="1">
        <apex:inputField value="{!Job_Poojan__c.Description__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Number_of_Positions__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Expires_On__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Active__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Salary_Offered__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Manager__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Job_Type__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Required_Skills__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Qualification_Required__c}"/>
        <apex:inputField value="{!Job_Poojan__c.Certification_Required__c}"/>
      </apex:pageBlockSection>
    </apex:pageBlock>
  </apex:form>
</apex:page>